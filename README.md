# CSCE 5300 Big Data Project

## Reproduction and Experimental Evaluation of Parallel K-Means for Big Data Clustering

This repository contains the implementation, validation, and experimental framework for a CSCE 5300 Big Data course project based on the research paper:

> **“Big Data Clustering Method Based on Parallel K-Means”**
> Haibo Liu, Yongbin Bai, Zhenhao Chen, and Zhenfeng Zhang
> 2024 IEEE 4th International Conference on Power, Electronics and Computer Applications (ICPECA)

The project focuses on independently reproducing the parallel K-Means architecture described in the reference paper, validating its behavior, and experimentally evaluating its scalability and clustering performance using Apache Spark.

The implementation is being developed incrementally. Each major component is tested independently before being integrated into the complete Parallel K-Means workflow.

---

## 1. Project Objective

Traditional K-Means becomes increasingly expensive as dataset size and dimensionality grow because each iteration repeatedly calculates distances between data points and cluster centers.

The reference paper addresses this limitation by distributing clustering work across Spark partitions and improving the initialization of the global cluster centers.

This project independently implements and evaluates that approach.

The primary research question is:

> **How does parallel K-Means compare with conventional K-Means in execution time, scalability, convergence behavior, and clustering quality as dataset size increases?**

The experimental evaluation will investigate:

- execution time,
- scalability,
- clustering convergence,
- sum of squared errors (SSE),
- clustering quality,
- partition behavior,
- initialization behavior,
- Spark execution configuration,
- and memory behavior where practical.

---

## 2. Reference-Paper Method

The selected paper proposes an improved Spark-based Parallel K-Means architecture.

The high-level process is:

1. Load and distribute the dataset using Spark.
2. Divide the data across Spark partitions.
3. Perform local K-Means clustering independently inside each partition.
4. Collect the cluster centers generated by the local clustering operations.
5. Re-cluster the collected candidate centers.
6. Use the resulting centers as the initialization for distributed global K-Means.
7. Broadcast the current global centers to Spark workers.
8. Assign distributed data points to their nearest centers.
9. Aggregate cluster statistics.
10. Update the global cluster centers.
11. Repeat until the convergence condition or maximum iteration count is reached.
12. Return the final centers and cluster assignments.

This strategy is intended to provide more stable initialization than selecting global centers randomly while also distributing the expensive assignment calculations across Spark workers.

---

## 3. Implemented Parallel Architecture

The current implementation follows this architecture:

```text
Raw Feature Vectors
        |
        v
    Spark RDD
        |
        v
+-------------------+
| Spark Partitions  |
+-------------------+
        |
        v
Local K-Means in Each Partition
        |
        v
Local Candidate Centers
        |
        v
Candidate-Center Aggregation
        |
        v
Global Initialization Centers
        |
        v
Broadcast Global Centers
        |
        v
Parallel Point Assignment
        |
        v
reduceByKey Aggregation
        |
        v
Global Center Update
        |
        v
Convergence Check
        |
        +------ Not Converged ------+
        |                           |
        +---------------------------+
        |
        v
Final Centers / SSE / Cluster Counts
```

The implementation is explicit rather than delegating clustering to Spark MLlib's built-in K-Means implementation. This allows the project to examine the distributed operations involved in the algorithm directly.

---

## 4. Major Implementation Components

### Spark Session Management

The Spark session layer provides controlled creation and shutdown of Spark sessions for demonstrations, tests, and experiments.

Development currently uses Spark local mode.

### Spark Partitioning

The partitioning layer validates how records are distributed across Spark partitions and provides utilities for inspecting partition-level behavior.

### Local K-Means

A deterministic local K-Means implementation is used inside individual Spark partitions.

The local implementation includes:

- input validation,
- squared Euclidean distance,
- nearest-center selection,
- center updates,
- deterministic seeded initialization,
- convergence detection,
- SSE calculation,
- and empty-cluster handling.

### Partition-Level Clustering

Each non-empty Spark partition performs local K-Means independently.

The result from each partition includes:

- local cluster centers,
- local convergence status,
- local iteration count,
- and local SSE information.

These local centers become candidate centers for the global initialization stage.

### Candidate-Center Aggregation

Candidate centers produced by all Spark partitions are collected and clustered again.

This second clustering operation produces exactly `k` centers that are used to initialize distributed global K-Means.

### Distributed Global Iteration

The global iteration performs the distributed assignment and update phase.

Current global centers are broadcast to Spark workers.

Each point is assigned to its nearest center and converted into cluster statistics containing:

- coordinate sums,
- point count,
- and SSE contribution.

Spark `reduceByKey` combines those statistics.

The driver then calculates updated global centers.

### Global Convergence Loop

The distributed iteration is repeated until:

- the convergence tolerance is satisfied, or
- the maximum number of iterations is reached.

The implementation records per-iteration information so that convergence behavior can later be analyzed experimentally.

### Complete Parallel K-Means Pipeline

The complete pipeline connects:

```text
Partition Clustering
        +
Candidate-Center Aggregation
        +
Distributed Global K-Means
```

The returned result includes final clustering information and intermediate metadata needed for experimental analysis.

---

## 5. Runtime Instrumentation

The pipeline records runtime measurements using Python's monotonic high-resolution performance counter.

Current measurements include:

- partition clustering time,
- candidate-center aggregation time,
- total initialization time,
- iterative distributed global K-Means clustering time,
- final-center evaluation time,
- and total pipeline runtime.

`global_clustering_seconds` measures only the repeated distributed assignment and center-update loop.

`final_evaluation_seconds` measures the separate Spark operation used to calculate final SSE and cluster counts against the returned centers.

The timing relationships are covered by automated tests.

### Controlled Benchmark Timing

A controlled benchmark layer now manages the Spark RDD lifecycle used during experimental execution.

The benchmark process is:

```text
Create RDD
    |
    v
Persist RDD
    |
    v
Materialize RDD with count()
    |
    v
Run Parallel K-Means
    |
    v
Collect Benchmark Metadata
    |
    v
Unpersist RDD
```

Materializing the RDD before the clustering pipeline begins prevents the initial RDD computation from being mixed into the clustering measurements.

The benchmark runner also releases the persisted RDD when execution finishes.

Failure-path cleanup is explicitly tested so that an RDD is unpersisted even when materialization raises an exception.

RDD lifecycle ownership remains in the experiment layer rather than inside the clustering algorithm. This keeps the core algorithm independent from the resource-management policy selected by the caller.

### Important Timing Note

Separating initialization, iterative global clustering, final evaluation, and RDD materialization provides clearer experimental timing boundaries.

The controlled benchmark foundation is now implemented, but report-grade runtime claims will require repeated benchmark trials under consistent configurations.

---

## 6. Project Structure

```text
CSCE5300-Big-Data-Project/
|
|-- README.md
|-- .gitignore
|
|-- src/
|   |-- __init__.py
|   |-- experiments/
|   |   |-- __init__.py
|   |   |-- benchmark_runner.py
|   |   `-- synthetic_data.py
|   `-- parallel/
|       |-- __init__.py
|       |-- center_aggregation.py
|       |-- global_iteration.py
|       |-- global_kmeans.py
|       |-- local_kmeans.py
|       |-- parallel_kmeans.py
|       |-- partition_clustering.py
|       |-- partitioning.py
|       `-- spark_session.py
|
|-- scripts/
|   |-- __init__.py
|   |-- demo_parallel_kmeans.py
|   `-- demo_partitioning.py
|
|-- tests/
|   |-- __init__.py
|   |-- conftest.py
|   |-- experiments/
|   |   |-- __init__.py
|   |   |-- test_benchmark_runner.py
|   |   `-- test_synthetic_data.py
|   `-- parallel/
|       |-- __init__.py
|       |-- test_center_aggregation.py
|       |-- test_global_iteration.py
|       |-- test_global_kmeans.py
|       |-- test_local_kmeans.py
|       |-- test_parallel_kmeans.py
|       |-- test_partition_clustering.py
|       |-- test_spark_initialization_pipeline.py
|       `-- test_spark_partition_integration.py
|
`-- results/
    |-- figures/
    |   `-- .gitkeep
    `-- raw/
        `-- .gitkeep
```

---

## 7. Development Environment

The current development environment uses:

```text
Operating System: macOS
Hardware: Apple Silicon
Apache Spark: 4.2.0
Scala: 2.13.18
Java: OpenJDK 21
Python Virtual Environment: Python 3.13
PySpark: 4.2.0
Testing Framework: pytest
```

The project currently executes Spark locally rather than on a physical multi-machine cluster.

Local Spark execution allows the implementation to test different levels of parallelism while maintaining a reproducible development environment.

---

## 8. Environment Setup

Create and activate the Python virtual environment:

```bash
/opt/homebrew/bin/python3.13 -m venv .venv
source .venv/bin/activate
```

Configure the Spark environment:

```bash
export SPARK_HOME=/opt/spark
export PYTHONPATH="$SPARK_HOME/python:$(find "$SPARK_HOME/python/lib" -maxdepth 1 -name 'py4j*.zip' -print -quit)"
```

Verify Python:

```bash
python --version
```

Verify Spark:

```bash
spark-submit --version
```

Verify PySpark:

```bash
python -c "import pyspark; print(pyspark.__version__)"
```

The expected PySpark version for the current environment is:

```text
4.2.0
```

---

## 9. Running the Tests

Activate the virtual environment and configure Spark before running the test suite:

```bash
source .venv/bin/activate
export SPARK_HOME=/opt/spark
export PYTHONPATH="$SPARK_HOME/python:$(find "$SPARK_HOME/python/lib" -maxdepth 1 -name 'py4j*.zip' -print -quit)"
```

Run the complete test suite:

```bash
python -m pytest -v --tb=short
```

At the current stable milestone, the project contains **87 passing tests**.

The test suite covers:

- local K-Means mathematics,
- input validation,
- distance calculations,
- center updates,
- SSE calculations,
- deterministic initialization,
- partition-level clustering,
- candidate-center aggregation,
- Spark partition integration,
- distributed global iterations,
- convergence behavior,
- complete Parallel K-Means execution,
- reproducibility,
- runtime instrumentation,
- separated clustering and final-evaluation timing,
- deterministic synthetic dataset generation,
- controlled benchmark execution,
- RDD persistence,
- RDD materialization,
- benchmark metadata,
- RDD cleanup during normal execution,
- RDD cleanup during materialization failure,
- and shared Spark test-session lifecycle behavior.

Spark integration and experiment tests use a shared session-scoped pytest fixture.

Using one controlled Spark test session avoids competing Spark contexts and provides consistent setup and teardown across the complete regression suite.

The current validated regression checkpoint is:

```text
87 tests collected
87 tests passed
```

---

## 10. Running the Demonstrations

### Spark Partitioning Demonstration

Run:

```bash
python -m scripts.demo_partitioning
```

This demonstration verifies record distribution across Spark partitions and validates partition-level processing.

A validated development run used:

```text
Records: 1000
Partitions: 4
Records per partition: 250
Total record sum: 500500
```

The partition validation completed successfully.

### Complete Parallel K-Means Demonstration

Run:

```bash
python -m scripts.demo_parallel_kmeans
```

The current demonstration uses a small synthetic dataset with four clearly separated clusters.

A validated execution produced:

```text
Records: 24
Features: 2
Clusters: 4
Partitions: 4
Candidate centers: 16
Global iterations: 2
Converged: True
Final SSE: approximately 0.906667
```

Each final cluster contained six records.

This demonstration is intended for functional validation and should **not** be treated as a large-scale performance benchmark.

---

## 11. Experimental Methodology

The experimental framework will evaluate the implementation using controlled configurations.

Planned independent variables include:

- dataset size,
- feature count,
- number of clusters (`k`),
- Spark master configuration,
- RDD partition count,
- random seed,
- maximum iterations,
- convergence tolerance,
- and trial number.

Measurements will include:

- partition clustering time,
- center aggregation time,
- initialization time,
- iterative global clustering time,
- final evaluation time,
- total runtime,
- iterations completed,
- convergence status,
- SSE,
- and cluster counts.

Initial local scalability experiments are planned for:

```text
local[1]
local[2]
local[4]
```

Dataset sizes will increase progressively so that correctness, memory behavior, and runtime characteristics can be validated at each scale before larger experiments are attempted.

Potential synthetic experiment sizes include:

```text
1,000 records
10,000 records
50,000 records
100,000 records
500,000 records
1,000,000 records
```

Larger experiments will only be attempted when the development machine can execute them reliably.

### Controlled Benchmark Execution

The benchmark runner records core experimental metadata including:

- number of records,
- number of features,
- number of clusters,
- number of Spark partitions,
- Spark master configuration,
- random seed,
- materialized record count,
- clustering results,
- convergence information,
- and runtime information.

The input RDD is persisted and materialized before clustering begins.

This establishes a consistent experimental boundary between dataset/RDD preparation and the measured clustering operations.

### Repeated Trials

Report-grade performance comparisons will use repeated trials rather than relying on a single execution.

Repeated experiments will allow runtime results to be summarized using appropriate descriptive statistics such as mean and standard deviation.

The same dataset configuration, cluster count, convergence settings, and Spark configuration will be maintained when comparing execution modes.

---

## 12. Experimental Results

Generated raw experiment data will be stored under:

```text
results/raw/
```

Generated figures will be stored under:

```text
results/figures/
```

Raw generated CSV and JSON experiment files are excluded from Git by default to prevent benchmark output from unnecessarily increasing repository size.

Final report-ready results and figures may be selectively preserved when the experimental phase is complete.

No report-grade performance conclusions should be drawn from small development demonstrations or instrumentation sanity checks.

---

## 13. Reproducibility

Experiments are designed to record enough metadata to reproduce a run.

Relevant configuration information includes:

```text
dataset
record count
feature count
number of clusters
Spark master
partition count
random seed
maximum iterations
convergence tolerance
trial number
```

The synthetic dataset generator is deterministic when given the same configuration and random seed.

Synthetic cluster sizes are balanced as evenly as possible, with any remainder assigned deterministically.

The benchmark layer also verifies that the materialized RDD contains the expected number of records before clustering begins.

This allows experimental results to be traced back to the configuration that produced them.

---

## 14. Testing Strategy

Development follows a staged workflow.

Each major algorithmic component is:

1. implemented independently,
2. tested with focused unit tests,
3. validated with Spark integration tests where appropriate,
4. checked against the complete regression suite,
5. reviewed for unintended changes,
6. and committed only after validation.

The project uses verbose pytest output:

```bash
python -m pytest -v --tb=short
```

The Spark test infrastructure uses one shared session-scoped fixture so that integration and experiment tests do not repeatedly create and stop competing Spark contexts.

The benchmark layer includes explicit cleanup testing for failure paths in addition to successful execution.

This staged testing strategy reduces the risk of introducing regressions while the distributed implementation and experimental framework continue to expand.

---

## 15. Current Project Status

Completed milestones:

```text
[Completed] Spark development environment
[Completed] Spark partitioning validation
[Completed] Deterministic local K-Means
[Completed] Partition-level Spark clustering
[Completed] Candidate-center aggregation
[Completed] Distributed global K-Means iteration
[Completed] Global convergence loop
[Completed] Complete Parallel K-Means pipeline
[Completed] End-to-end Spark demonstration
[Completed] Runtime instrumentation
[Completed] Runtime validation
[Completed] Deterministic synthetic benchmark datasets
[Completed] Separate iteration and evaluation timing
[Completed] Controlled benchmark runner
[Completed] RDD persistence strategy
[Completed] RDD materialization before clustering
[Completed] Benchmark RDD cleanup
[Completed] Shared Spark test-session lifecycle
[Completed] 87-test regression suite
```

Current development milestone:

```text
[In Progress] Experimental benchmark foundation
```

Planned work:

```text
[Planned] Controlled 1,000-record benchmark validation
[Planned] Experiment metadata schema
[Planned] local[1] / local[2] / local[4] experiments
[Planned] Partition-count experiments
[Planned] Repeated benchmark trials
[Planned] Progressive dataset scaling
[Planned] Conventional K-Means comparison
[Planned] KDD Cup 99 experiment
[Planned] Purity evaluation
[Planned] SSE comparison
[Planned] Runtime and speedup analysis
[Planned] Memory investigation
[Planned] Comparison with reference-paper results
[Planned] Report-ready figures and tables
```

---

## 16. Current Experimental Limitations

### Local Spark Execution

The current environment uses Spark local mode on one physical machine.

Although:

```text
local[1]
local[2]
local[4]
```

allow different levels of parallel execution, these configurations are not equivalent to the three-machine Spark cluster used by the reference paper.

Results from local Spark experiments will therefore be described as local parallel scalability experiments rather than direct reproductions of the paper's physical cluster configuration.

### Timing Definition

The runtime instrumentation separates:

- initialization,
- iterative global clustering,
- final-center evaluation,
- and total pipeline runtime.

The controlled benchmark runner additionally persists and materializes the experiment RDD before the clustering pipeline begins.

This reduces distortion caused by including initial RDD computation in the measured clustering phase.

Report-grade runtime comparisons still require controlled repeated trials and consistent experimental configurations.

### Convergence Definition

The current implementation uses **maximum squared center movement** as its convergence criterion.

The reference paper describes convergence using a **change in clustering cost**.

This methodological difference must either be aligned before final paper-comparison experiments or clearly documented when interpreting the results.

Therefore, the current implementation should not yet be described as an exact reproduction of every convergence detail in the reference paper.

### Dataset Persistence

RDD persistence is now controlled by the benchmark layer rather than the clustering algorithm.

The benchmark runner:

```text
creates the RDD
        |
persists it
        |
materializes it
        |
executes clustering
        |
collects benchmark metadata
        |
unpersists the RDD
```

Cleanup also occurs when RDD materialization fails.

This design keeps resource lifecycle management outside the core Parallel K-Means algorithm while providing a controlled environment for repeated Spark operations.

### Programming-Language Requirement

The course project requires the reproduction to use a programming language different from the language used by the selected paper.

The reference paper describes its Spark implementation and mentions `sklearn` when discussing generation of one experimental dataset, but the programming language used for the paper's Spark-Kmeans implementation has not yet been clearly identified from the paper.

This requirement must be confirmed before the final implementation language is treated as approved for submission.

---

## 17. Team Workflow

The project is divided into four primary areas.

**Research and Algorithm Analysis** focuses on understanding the reference paper, documenting the algorithm, developing pseudocode, and connecting the implementation to the original methodology.

**Data and Preprocessing** focuses on datasets, cleaning, feature preparation, standardization, reproducible data pipelines, and preparation of experimental inputs.

**Spark and Parallel Systems** focuses on Spark configuration, partitioning, local partition clustering, distributed aggregation, global clustering, performance instrumentation, scalability, execution architecture, and benchmark infrastructure.

**Experiments and Evaluation** focuses on experimental comparisons, clustering metrics, visualization, statistical summaries, speedup analysis, and report-ready results.

The intended workflow is:

```text
Research / Algorithm
        |
        v
Data / Preprocessing
        |
        v
Spark Parallel Implementation
        |
        v
Experiments / Evaluation
        |
        v
Report and Presentation
```

The components are designed so that algorithm, data, systems, and evaluation work can be developed independently while sharing a common experimental pipeline.

---

## 18. Academic Purpose

This repository was created for the **CSCE 5300 – Introduction to Big Data and Data Science** course project.

The project focuses on independently reproducing, testing, and experimentally evaluating ideas from the selected research paper rather than presenting the original authors' implementation as our own.

The selected paper is:

> Haibo Liu, Yongbin Bai, Zhenhao Chen, and Zhenfeng Zhang,
> **“Big Data Clustering Method Based on Parallel K-Means,”**
> 2024 IEEE 4th International Conference on Power, Electronics and Computer Applications (ICPECA), 2024.

The final project will compare independently generated experimental results with results reported by the reference paper.

Reported results from the paper and results produced by this repository will be clearly distinguished.

All final experimental claims, figures, numerical comparisons, and conclusions about this implementation will be based on reproducible executions of the code contained in this project.